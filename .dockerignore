# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
env/
venv/
ENV/
*.egg-info/
dist/
build/

# Sessions
sessions/*.session
sessions/*.session-journal

# IDE
.vscode/
.idea/
*.swp
*.swo

# Git
.git/
.gitignore

# Docs
*.md
!README.md

# Environment
.env.local
.env.test

# Frontend is built from its own context (./frontend in docker-compose.yml),
# so excluding it here only strips it from the backend image — which was
# baking in ~444MB of frontend source for no reason and filling host disk
# across redeploys.
frontend/

# Node modules (any stray copies)
node_modules/
package.json
package-lock.json

