# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
env/
venv/
ENV/
*.egg-info/
dist/
build/

# Sessions
sessions/*.session
sessions/*.session-journal

# IDE
.vscode/
.idea/
*.swp
*.swo

# Git
.git/
.gitignore

# Docs
*.md
!README.md

# Environment
.env.local
.env.test

# Frontend: keep the SOURCE in the context so Dockerfile.fullstack stage 1 can
# build it (only ~778K), but never ship node_modules / prior build output.
frontend/node_modules/
frontend/build/

# Node modules (any stray copies). Root package files only — keep
# frontend/package*.json so Dockerfile.fullstack stage 1 can `npm ci`.
node_modules/
/package.json
/package-lock.json

